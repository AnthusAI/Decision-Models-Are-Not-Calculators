# Results

This directory will contain generated summaries and release metadata after the
registered model runs. The per-response JSONL records are committed in
`data/answers/`. The release record must identify incomplete runs and include
SHA-256 checksums; no partial run should be described as a three-model result.

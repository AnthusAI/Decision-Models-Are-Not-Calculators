Before running, record exact Python and dependency versions, OS, hardware, and
model/service revisions in the result manifest. Use Python 3.12. The Kev service
and Laya weights must be prepared from the immutable revisions documented in
`docs/engines.md`; configure local service/checkpoint paths with environment
variables rather than assuming host-specific locations.
